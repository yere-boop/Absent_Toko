<div class="form-page">
  <div class="form-page-header">
    <a href="/employees" class="btn btn-secondary btn-sm">
      <i data-lucide="arrow-left"></i>
      Kembali
    </a>
  </div>

  <div class="card form-card">
    <div class="card-header">
      <h2 class="card-title">
        <i data-lucide="edit"></i>
        Edit Data Karyawan
      </h2>
      <span class="badge-count"><%= employee.employee_code %></span>
    </div>
    <div class="card-body">
      <form action="/employees/<%= employee.id %>?_method=PUT" method="POST" id="employeeEditForm" novalidate>
        <div class="form-grid">
          <div class="form-group form-full">
            <label for="name">Nama <span class="required">*</span></label>
            <input type="text" id="name" name="name" class="form-control"
              placeholder="Nama lengkap karyawan"
              value="<%= employee.name %>" required>
          </div>

          <div class="form-group form-full">
            <label for="position">Jabatan <span class="required">*</span></label>
            <input type="text" id="position" name="position" class="form-control"
              placeholder="Contoh: Staff, Supervisor, Manager"
              value="<%= employee.position %>" required>
          </div>
        </div>

        <div class="form-actions">
          <a href="/employees" class="btn btn-secondary">Batal</a>
          <button type="submit" class="btn btn-primary">
            <i data-lucide="save"></i>
            Simpan Perubahan
          </button>
        </div>
      </form>
    </div>
  </div>
</div>
